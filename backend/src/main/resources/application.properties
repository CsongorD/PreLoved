
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/preloved}
spring.datasource.username=${DATABASE_USERNAME:postgres}
spring.datasource.password=${DATABASE_PASSWORD:031113}
spring.jpa.properties.hibernate.format_sql=true

# Automatically create/update tables based on entity classes
spring.jpa.hibernate.ddl-auto=update

spring.datasource.driver-class-name= org.postgresql.Driver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
logging.level.org.springframework.security = TRACE

jwt.token.validity=18000
jwt.signing.key=lacinagyoneroskulcsalacinagyoneroskulcsalacinagyoneroskulcsa
jwt.authorities.key=roles
jwt.token.prefix=Bearer
jwt.header.string=Authorization